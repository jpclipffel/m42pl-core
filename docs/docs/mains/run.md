# Run

Runs a M42PL pipeline from a file.

```shell
m42pl run <source_file>
```

## Arguments

> **Tip**  
> Run `m42pl repl -h` or `m42pl repl --help` to get help

> **TODO**  
> Document REPL arguments
